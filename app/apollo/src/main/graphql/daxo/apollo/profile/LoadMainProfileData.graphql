query LoadMainProfileData {
    Viewer {
        id
        name
        about
        bannerImage
        unreadNotificationCount
        siteUrl
        donatorTier
        donatorBadge
        avatar {
            ...userAvatar
        }
    }
}